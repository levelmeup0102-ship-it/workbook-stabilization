# 2026-04-03 리팩토링 기록

## 완료된 작업

### Phase 1: 즉시 수정

- [x] **APP_PASSWORD 기본값 하드코딩 제거**
  - 현재: main.py: `os.getenv()`삭제
  - 변경: 기본값 제거, 환경변수 필수화 (미설정 시 서버 시작 실패)

- [x] **content_match 정답 셔플 오류 수정** — 코드 확인 결과 이미 (text, is_correct) 쌍 셔플로 구현됨 (수정 불필요)

### Phase 1.5: 로깅 시스템 구축

- [x] **Simple Loguru 로깅 시스템 구축 (`app/log/`)**
  - app/log/logging.py: setup_logger() — console sink only, 레벨별 색상
  - app/log/events.py: 얇은 로깅 헬퍼 (앱 코드에서 loguru 직접 사용 금지)

- [x] **환경 변수 관리 (`app/config.py`)**
  - Pydantic BaseSettings + Field + @lru_cache 싱글턴
  - 대상: APP_PASSWORD, ANTHROPIC_API_KEY, SUPABASE_URL, SUPABASE_KEY, PORT, ENV
  - main.py, pipeline.py, supa.py, app/log/logging.py의 os.getenv() → get_settings().XXX 교체
  - 필수 변수 누락 시 서버 시작 실패 (ValidationError), 민감 값 SecretStr 보호
  - requirements.txt에 pydantic-settings 추가

---

## 기타 결정 사항

- 디렉토리 구조 확정 (app/ 하위 구조, prompts/ 폴더 → prompts.py 단일 파일, core/ → log/)
- 로깅 Simple Plan 수정 (파일 sink 제거, 얇은 헬퍼 레이어 추가)
- 기존 구조화 로깅 설계는 고도화로 이동
- 고도화 항목 추가: 환경 분리(dev/staging/prod), 시크릿 정기 교체, 구조화 로깅 고도화
- 작업 프로세스 확정: 수정 내용 설명 → 컨펌 → 진행 → 커밋 메시지 제안

## 다음 세션 시작 시

- Phase 1.5 남은 항목: BaseModel 기반 API 응답 스키마, 커스텀 에러 클래스 (보류)
- 이후 Phase 2: 데이터 레이어 기반 구축부터 진행
