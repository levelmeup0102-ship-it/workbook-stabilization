# 2026-03-27 리팩토링 기록

## 해결된 작업

### 1. step2(Stage 1 어휘 테스트) 답안 매칭 이슈 해결

- **이슈**: 답안지 A/B/C파트의 정답을 DB에서 조회해야 하는가?
- **결론**: DB 조회 불필요. 같은 step1 결과(vocab 14개)에서 코드로 매칭하면 됨 (비용 0)
- test_a/b/c도 Claude에게 요청할 필요 없음 → vocab에서 랜덤 추출 알고리즘으로 전환 확정
- 정답도 vocab의 meaning/synonyms/word로 자동 생성

### 2. step1 Claude 요청 항목 축소 확정

- 기존 8개 → **2개만** Claude 필요: `vocab`, `key_sentences`
- 제거 대상: translation, sentences, sentence_translations, test_a, test_b, test_c
  - translation/sentence_translations → 업로드 시 분리 저장
  - sentences → regex로 이미 대체 중
  - test_a/b/c → 코드 알고리즘으로 전환

### 3. 전체 프로젝트 파일 전수 점검 완료

점검 대상 (총 14개 파일):
- main.py, pipeline.py, supa.py, sitecustomize.py
- template.html, static/index.html, static/manifest.json
- requirements.txt, Dockerfile, Procfile, nixpacks.toml, runtime.txt
- .gitignore, CLAUDE.md

### 4. 프론트엔드 파일 분리 범위 조정

- 기존: HTML + CSS + JS 분리 (css/index.css, js/api.js, js/ui.js, js/auth.js, js/main.js)
- 변경: **CSS 분리만** (css/index.css)
- 이유: 페이지 1개, JS 700줄은 한 파일 관리 가능. JS 분리 시 import 순서/전역 변수 관리 부담
- JS 분리 시점: 코드 1500줄 이상 또는 페이지 추가 시

---

## 신규 발견 및 등록된 작업

### 안정화 (신규 20건 → tasks.md P1에 등록)

**보안 (6건)**
- APP_PASSWORD 기본값 하드코딩 제거
- XSS 취약점 수정 (index.html)
- 입력값 길이 제한 추가
- CORS allow_origins 제한
- 인증 실패 메시지 통일
- book 파라미터 경로 탐색 방지 (Pydantic과 함께 처리)

**에러 처리 (3건)**
- except Exception 남용 + silent failure 제거
- supa.py JSON 파싱 에러 처리
- 토큰 만료 처리 일관화 (index.html)

**데이터 무결성 (3건)**
- 삭제 작업 원자성 보장
- content_match 정답 셔플 오류 수정
- 캐시 로드 후 필드 검증

**코드 품질 (4건)**
- 경로 처리 일관성 (절대/상대 혼용)
- 임시 파일 정리 보장
- JSON 파싱 탐욕적 매칭 수정
- template.html CSS 클래스명 불일치

**배포/설정 (5건)**
- requirements.txt 버전 고정
- anthropic SDK 추가
- Dockerfile/nixpacks curl 제거
- CMD/시작 명령 통일
- .gitignore 프로젝트 고유 경로 추가

### 고도화 (신규 7건 → tasks.md 고도화 섹션에 등록)

- 생성 작업 타임아웃/취소
- API 재시도 전략 고도화
- 설정 관리 중앙화
- 진행 상황 세분화
- 업로드 형식 프리뷰
- 지문 검색 기능
- PWA 아이콘 512px 추가

### 백엔드 재점검 결과

- 35건 보고 중 32건은 기존 항목으로 해소 확인
- 진짜 새로운 항목 3건 추가 등록 (CORS, 인증 메시지, 경로 탐색)

---

## 남은 작업

- 실제 구현 착수 (tasks.md P1부터)
